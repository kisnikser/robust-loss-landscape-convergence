model:
  class: mlp
  input_channels: 3
  input_sizes: [32, 32]
  hidden: 128
  layers_num: 6
  variable_param: hidden
  variable_param_grid: [64, 128, 256, 512, 1024]
  classes: 100

logging:
  savefig_path: 'results/CIFAR100/mlp_hidden_sigm8.png'

dataset:
  name: 'CIFAR100'
  load_path: 'data/'
  transform_params:
    mean: [0.5, 0.5, 0.5]
    std: [0.5, 0.5, 0.5]
  batch_size: 64
  size: -1

train_params:
  optimizer: Adam
  learning_rate: 3.0e-4
  num_epochs: 80

device: 'cuda'

delta_vis_params:
  mode: random-subspace-proj
  mode_params:
    estim_func: abs
    sigma: 8
    dim: 2
  num_samples: 64

visualize:
  smooth_window: 10
  train_loss_smooth_window: 20
