model:
  class: conv
  hidden_channels: 64
  layers_num: 10
  kernel_size: 3
  input_channels: 3
  output_channels: 16
  input_sizes: [32, 32]
  variable_param: layers_num
  variable_param_grid: [1, 3, 6, 8, 9, 10, 12, 15, 18, 21]
  padding: 1
  classes: 10

logging:
  models_checkpoint_path: checkpoints/change_layers_num
  savefig_path: 'results/CIFAR10/conv_layers_num_sigm3.png'

dataset:
  name: 'CIFAR10'
  load_path: 'data/'
  transform_params:
    mean: [0.5, 0.5, 0.5]
    std: [0.5, 0.5, 0.5]
  batch_size: 64
  size: -1

train_params:
  optimizer: Adam
  learning_rate: 3.0e-4
  num_epochs: 30

device: 'cuda'

delta_vis_params:
  mode: random-subspace-proj
  mode_params:
    estim_func: abs
    sigma: 3
    dim: 2
  num_samples: 32

visualize:
  smooth_window: 10
  train_loss_smooth_window: 20
